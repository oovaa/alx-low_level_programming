#include "main.h"
/**
 * add - a function that adds two integers and returns the result.
 *
 *@a:  first perametot
 *@b:  second perametot
 * Return: Always 0 (Success)
*/

int add(int a, int b)
{
int sum = a + b;
return (sum);
}
